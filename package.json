{
  "name": "ai-study-assistant",
  "version": "1.0.0",
  "description": "AI Study Assistant Root Workspace",
  "scripts": {
    "install:all": "npm install --include=dev && npm install --include=dev --prefix Backend && npm install --include=dev --prefix Frontend",
    "dev": "concurrently -n \"api,app\" -c \"bgBlue,bgGreen\" \"npm run dev --prefix Backend\" \"npm run dev --prefix Frontend\"",
    "build:frontend": "npm run build --prefix Frontend",
    "start": "cross-env NODE_ENV=production node Backend/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  }
}
